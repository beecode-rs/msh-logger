when:
  - event: pull_request
    branch:
      - main
      - dependabot-update

steps:
  - name: lint-test
    image: node:22.22.1-alpine3.23
    when:
      - path: ['src/**']
    commands:
      - npm ci
      - npm run lint
      - npm run test:unit
